<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AzkarApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/teal_primary</item>
        <item name="colorOnPrimary">@color/textOnPrimary</item>
        <item name="colorPrimaryContainer">@color/teal_dark</item>
        
        <!-- Background and surface colors. -->
        <item name="android:windowBackground">@color/backgroundColor</item>
        <item name="colorSurface">@color/surfaceColor</item>
        <item name="colorOnSurface">@color/textPrimary</item>
        <item name="colorOnSurfaceVariant">@color/textSecondary</item>
        
        <!-- Status bar and navigation bar. -->
        <item name="android:statusBarColor">@color/backgroundColor</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/backgroundColor</item>
    </style>

    <style name="Theme.AzkarApp" parent="Base.Theme.AzkarApp" />
</resources>